# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node / Electron
node_modules/
dist/
build/
out/
release/
.next/
*.log

# electron-builder cache
.cache/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Secrets / local config
.env
.env.local
.env.*.local
*.key
*.pem
secrets.json

# Local databases
*.sqlite
*.sqlite3
*.db

# IST runner output (workspaces, snapshots, dbs, per-iter results)
# — kept out of git because they're large and reproducible from
# the prompts under benchmarks/ist/prompts/.
benchmarks/ist/runs/

# IST per-iter metric data files (the .ts source under
# benchmarks/ist/metrics/ stays tracked; only the per-cell JSON
# data + golden captures generated by `pnpm ist-metrics` are
# excluded). Final processed artifacts get committed manually
# alongside the V0.2.11 writeup.
benchmarks/ist/metrics/polycoder-full/
benchmarks/ist/metrics/polycoder-coder-only/
benchmarks/ist/metrics/lovable/

# IST aggregated results — regenerate locally via
# `pnpm ist-aggregate`. Committed alongside V0.2.11 writeup.
benchmarks/ist/results/

# Coverage / cache
.coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/
